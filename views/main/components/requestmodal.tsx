import React, {useEffect} from "react"
import * as Be from "../../../firebase/common"
import { View } from "react-native"

export default function RequestModal(locations: string[]) {
  
  const handleRequestSend = () => {
    
  }
  return <View></View>
}